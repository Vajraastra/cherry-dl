if not contains "/run/media/system/Kilaya/githubs/cherry-dl/.tools" $PATH
    # Prepending path in case a system-installed binary needs to be overridden
    set -x PATH "/run/media/system/Kilaya/githubs/cherry-dl/.tools" $PATH
end
